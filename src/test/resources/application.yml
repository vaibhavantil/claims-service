spring:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        order_by.default_null_ordering: last
  datasource:
    url: jdbc:h2:mem:claims;MODE=PostgreSQL;DB_CLOSE_ON_EXIT=FALSE
